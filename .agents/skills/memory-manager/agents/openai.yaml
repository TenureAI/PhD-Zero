interface:
  display_name: "Memory Manager"
  short_description: "Maintain working todo memory and reusable research records."
  default_prompt: "Use memory manager to keep working state fresh, track active/done/blocked todos, write evidence-linked episode/procedure/insight records, and use the shared memory repo only as a read-only retrieval source unless an approved export is happening."
